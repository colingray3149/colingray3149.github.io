---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
  <main class="wrap stack-32">
    <!-- HERO -->
    <section class="hero stack-16">
      <img
        class="avatar"
        src="/assets/colin-gray-portrait.jpg"
        alt="Portrait of Colin Gray"
        width="164"
        height="164"
        loading="eager"
      />

      <div class="stack-6 center">
        <h1 class="name">Colin Gray</h1>
        <p class="tagline">
          Caltech undergraduate · Chemical Engineering · Combustion & CFD · ML for reaction mechanisms
        </p>

        <nav class="social">
          <a href="https://github.com/colingray3149" aria-label="GitHub">GitHub</a>
          <a href="https://www.linkedin.com/in/YOUR-LINK" aria-label="LinkedIn">LinkedIn</a>
          <a href="mailto:YOUR@EMAIL.com" aria-label="Email">Email</a>
        </nav>
      </div>
    </section>

    <!-- CARDS -->
    <section class="grid">
      <article class="card">
        <h2>About Me</h2>
        <p>
          I’m a Caltech ChemE focused on combustion modeling and CFD. Recent work includes a scramjet
          cold-flow in ANSYS Fluent, methane mechanism reduction with PyTorch + Cantera, and FDS/PyroSim
          fire simulations. I like building rigorous, reproducible pipelines and explaining them clearly.
        </p>
      </article>

      <article class="card">
        <h2>Research Interests</h2>
        <ul class="bullets">
          <li>Physics-informed ML / operator learning</li>
          <li>Mechanism reduction & uncertainty</li>
          <li>High-speed reacting flows</li>
        </ul>
      </article>

      <article class="card span-2">
        <h2>Recent Updates</h2>
        <ul class="updates">
          <li><strong>Aug ’25</strong> • Scramjet 2D cold-flow mesh + first residual/CL/CD checks</li>
          <li><strong>Aug ’25</strong> • L1-pruned GRI-Mech model: RMSE ↓, inference ×20 faster</li>
          <li><strong>Jul ’25</strong> • FDS dresser fire: sensitivity to HRRPUA & vent placement</li>
        </ul>
      </article>
    </section>

    <!-- CTA -->
    <section class="cta">
      <a class="btn" href="/work/">See Projects</a>
      <a class="btn ghost" href="/Colin-Gray-Resume.pdf">Download CV</a>
    </section>
  </main>
</BaseLayout>

<style>
  /* ==== Layout helpers ==== */
  .wrap { max-width: 1100px; margin-inline: auto; padding: 3rem 1.25rem; }
  .stack-32 > * + * { margin-top: 2rem; }
  .stack-16 > * + * { margin-top: 1rem; }
  .stack-6  > * + * { margin-top: 0.375rem; }
  .center { text-align: center; }

  /* ==== Hero ==== */
  .hero { display: grid; place-items: center; padding-top: 1rem; }
  .avatar {
    border-radius: 50%;
    box-shadow: var(--shadow-sm);
    display: block;
  }
  .name   { font-size: clamp(2rem, 3.5vw, 2.75rem); line-height: 1.1; }
  .tagline{ color: var(--gray-300); max-width: 62ch; margin-inline: auto; }

  .social { display: inline-flex; gap: 0.875rem; font-size: 0.95rem; }
  .social a { color: var(--gray-200); text-decoration: none; border-bottom: 1px solid transparent; }
  .social a:hover { border-bottom-color: currentColor; }

  /* ==== Cards grid ==== */
  .grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }
  @media (min-width: 860px) {
    .grid { grid-template-columns: 1fr 1fr; }
    .span-2 { grid-column: span 2; }
  }

  .card {
    border: 1px solid var(--gray-800);
    background: var(--surface, rgba(255,255,255,0.02));
    border-radius: 18px;
    padding: 1.25rem 1.1rem;
    box-shadow: var(--shadow-xs);
  }
  .card h2 { margin: 0 0 .5rem 0; font-size: 1.25rem; }
  .bullets { margin: 0; padding-left: 1.1rem; }
  .updates { margin: 0; padding-left: 1.1rem; line-height: 1.6; }

  /* ==== CTA buttons ==== */
  .cta { display: flex; gap: .75rem; justify-content: center; padding: .5rem 0 1rem; }
  .btn {
    padding: .6rem .95rem;
    border-radius: 12px;
    border: 1px solid var(--gray-700);
    text-decoration: none;
    color: var(--gray-050);
    box-shadow: var(--shadow-xs);
  }
  .btn:hover { border-color: var(--gray-600); }
  .btn.ghost { background: transparent; }
</style>
